Finance Tracker — a simple personal finance frontend built with Next.js (App Router) and Tailwind.

Run development server:

```bash
npm run dev
```

Open http://localhost:3000 and use the app to login/register and view your dashboard.
